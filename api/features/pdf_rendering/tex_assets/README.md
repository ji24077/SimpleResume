# Optional LaTeX project files

Place files that `\input{...}` or `\includegraphics` need **next to `resume.tex`** during compile.

Examples: `glyphtounicode.tex`, custom `.sty`, images, `.bib`.

The API copies allowed extensions from this folder into the compile temp directory.
