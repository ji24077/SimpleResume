"""Feature-area code layout under ``api/features/``.

- ``pdf_rendering`` — Docker LaTeX → PDF, preamble, tex assets
- ``generation`` — prompts + structured resume → LaTeX
- ``resume_pipeline`` — lint/compile/pages/ATS gates

Branch naming map: ``docs/FEATURE_BRANCHES.md``.
"""
